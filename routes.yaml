routes:
  /membership/:
    template: members/membership
    data: page.membership
  /signup/:
    template: members/signup
    data: page.signup
  /signin/:
    template: members/signin
    data: page.signin
  /subscribe/:
    template: members/subscribe
    data: page.subscribe
  /account/:
    template: members/account
    data: page.account

collections:
  /:
    permalink: /{slug}/
    template: index

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/